name: WF_Tag

on:
  workflow_dispatch:

jobs:
    setup:
        runs-on: [self-hosted,sjrunner-ec2]
        steps:
          - name: install nodejs
            run: sudo apt-get update && sudo apt install nodejs
          - name: check version name
            run: node --version
          - name: user name
            run: echo "runnner= $USER"